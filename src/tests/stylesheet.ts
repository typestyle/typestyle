import { reinit, stylesheet } from '../index';
import * as assert from 'assert';

describe("stylesheet", () => {
  it('basic support', () => {
    reinit();
    const styles = stylesheet({
      warning: {
        backgr
      }
    });

    // assert.equal(animationName, 'fade-in_f1gwuh0p');
  });

});
